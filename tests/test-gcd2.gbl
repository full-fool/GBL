class mymain extends Main{
int gcd(int a, int b) {
  while (a <= b)
    if (a > b) a = a - b;
    else b = b - a;
  return a;
}

int main()
{
  printlni(gcd(14,21));
  printlni(gcd(8,36));
  printlni(gcd(99,121));
  return 0;
}
}